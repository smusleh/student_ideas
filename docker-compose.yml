services:
  backend:
    build: ./backend
    environment:
      - DATABASE_URL=sqlite:////app/data/ideas.db
    volumes:
      - db_data:/app/data
    expose:
      - "8000"

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  db_data:
